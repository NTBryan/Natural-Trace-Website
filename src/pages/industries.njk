---
layout: base.njk
title: "Industries"
summary: "In-product authentication for nutraceuticals and dietary supplements, branded ingredients, functional foods and agri-food supply chains."
permalink: /industries/
---

<section class="home-industries" style="padding-top:9rem">
  <div class="container">
    <div class="industries-header">
      <span class="section-label">{{ industries.label }}</span>
      <h2 class="section-title">{{ industries.title }}</h2>
      <p class="section-subtitle" style="margin:0 auto">{{ industries.subtitle }}</p>
    </div>
    <div class="industries-grid">
      {% for industry in industries.industries %}
      <div class="industry-card delay-{{ loop.index }}" id="{{ industry.id }}">
        <img src="{{ industry.image }}" alt="{{ industry.image_alt }}" loading="lazy">
        <div class="industry-card-overlay"><h3>{{ industry.name }}</h3><p>{{ industry.description }}</p></div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="finalcta">
  <div class="container">
    <div class="finalcta-inner">
      <h2>{{ industries.cta_title }}</h2>
      <p>{{ industries.cta_description }}</p>
      <div class="finalcta-actions">
        <a href="{{ industries.cta_button_primary_url }}" class="btn btn-white" style="cursor:pointer">{{ industries.cta_button_primary_text }} &#8594;</a>
        <a href="{{ industries.cta_button_secondary_url }}" class="btn btn-ghost" style="cursor:pointer">{{ industries.cta_button_secondary_text }}</a>
      </div>
    </div>
  </div>
</section>

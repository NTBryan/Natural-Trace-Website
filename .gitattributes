# Normalise line endings in the repo. Windows checkouts still get CRLF for the
# files that need it; everything else is stored as LF.
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.png binary
*.jpg binary
*.mp4 binary
*.ico binary
